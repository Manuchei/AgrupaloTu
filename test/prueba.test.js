import { describe, expect, it } from "vitest";
import{createRandomGroups} from '../src/utils'

describe("empty spec", () => {
  it("empty", () => {
    const person = ['ivana', 'juana', 'ana', 'marina', 'diana']
    const data = createRandomGroups(persons, 2)
    
    //expect(r).toBeInstanceOf(Number)
  });
});


